# ✅ Быстрый старт (ИСПРАВЛЕНО)

## Проблема была решена!

**Проблема:** Python 3.13 слишком новый, пакеты не имеют готовых сборок.

**Решение:** Используем Python 3.12 в виртуальном окружении.

## 🚀 Запуск бота:

### Шаг 1: Активируйте виртуальное окружение

```bash
cd /Users/mitya/CursorAI/TGbotKasheeva_v3
source venv/bin/activate
```

Вы увидите `(venv)` в начале строки терминала.

### Шаг 2: Заполните BOT_TOKEN в .env

Откройте файл `.env` и замените:
```env
BOT_TOKEN=your_bot_token_here
```
на ваш реальный токен от @BotFather.

### Шаг 3: Запустите бота

```bash
python bot.py
```

Или если используете Python 3.12 напрямую:
```bash
python3.12 bot.py
```

## ✅ Что вы увидите:

```
Бот запущен!
```

## 🛑 Остановка бота:

Нажмите `Ctrl+C`

## 📝 Важно:

- **Всегда активируйте виртуальное окружение** перед запуском: `source venv/bin/activate`
- Зависимости уже установлены в `venv/`
- Используйте Python 3.12 (не 3.13)

## 🔄 Если нужно переустановить зависимости:

```bash
source venv/bin/activate
pip install -r requirements.txt
```

## 📋 Полная последовательность (один раз):

```bash
# 1. Перейдите в папку проекта
cd /Users/mitya/CursorAI/TGbotKasheeva_v3

# 2. Активируйте виртуальное окружение
source venv/bin/activate

# 3. Заполните BOT_TOKEN в .env файле
# Откройте .env и замените your_bot_token_here

# 4. Запустите бота
python bot.py
```

Готово! 🎉

