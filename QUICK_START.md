# Быстрый старт: Добавление Bot Token

## 📍 Где добавить Bot Token:

**Файл:** `.env` в корне проекта  
**Путь:** `/Users/mitya/CursorAI/TGbotKasheeva_v3/.env`

## 🔑 Как добавить:

### Вариант 1: Через редактор (рекомендуется)

1. Откройте файл `.env` в любом текстовом редакторе
2. Найдите или добавьте строку:
   ```env
   BOT_TOKEN=your_bot_token_here
   ```
3. Замените `your_bot_token_here` на ваш реальный токен от @BotFather
4. Сохраните файл

### Вариант 2: Через терминал

```bash
# Откройте файл в редакторе
nano .env
# или
code .env  # если используете VS Code
# или
vim .env
```

Затем найдите строку `BOT_TOKEN=` и замените значение.

### Вариант 3: Через команду (быстрый способ)

```bash
# Замените YOUR_TOKEN_HERE на ваш реальный токен
echo "BOT_TOKEN=YOUR_TOKEN_HERE" >> .env
```

⚠️ **Внимание:** Этот способ добавит токен в конец файла. Если `BOT_TOKEN` уже есть, лучше отредактировать файл вручную.

## 📝 Пример правильного заполнения:

```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
```

## ✅ Проверка:

После добавления токена проверьте:

```bash
# Проверьте что токен добавлен
grep BOT_TOKEN .env

# Запустите бота
python bot.py
```

Если токен правильный, вы увидите: `Бот запущен!`

## 🔐 Как получить Bot Token:

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot` (если бот новый) или `/token` (если бот уже создан)
4. Следуйте инструкциям
5. Скопируйте токен (формат: `числа:буквы_и_цифры`)

## ⚠️ Важно:

- ❌ Не добавляйте пробелы или кавычки вокруг токена
- ❌ Не добавляйте символ `@` перед токеном  
- ✅ Токен должен быть в формате: `1234567890:ABCdef...`
- ✅ Файл `.env` уже в `.gitignore` - не попадет в Git

## 📋 Другие обязательные настройки в .env:

После `BOT_TOKEN` также нужно заполнить:

```env
MODERATOR_ID=123456789  # Ваш Telegram ID (узнайте у @userinfobot)
CHANNEL_USERNAME=doctor_kashcheeva  # Уже указан правильно
```

Остальные настройки (EMAIL, SMTP) опциональны.

